# Betacom - Cypress.io (JavaScript)
## Setup
Install Cypress framework:
```
npm install cypress --save-dev
```

## Run
* Headless mode:
```
npx cypress run
```

* Headed mode:
```
npx cypress open
```

## Output
Output files (in `.json` format) are saved in the `output` folder